# Hail Frontend

Simple React app to interact with the Hail backend.
You can add leads and hail events and see them listed in real time.

## Running locally

```
pnpm install --ignore-workspace
pnpm start
```

The app assumes the backend is running on `http://localhost:3001`.
It lets you add and view leads as well as hail events from a mobile-friendly UI.
