// Export all core components of the visual testing framework
const TaskQueue = require('./TaskQueue');
const AgentRegistry = require('./AgentRegistry');
const Orchestrator = require('./Orchestrator');

module.exports = {
    TaskQueue,
    AgentRegistry,
    Orchestrator,
};